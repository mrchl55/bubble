import React, { useState, useRef, useEffect } from 'react';

const tagsList: string[] = ['React', 'Next.js', 'Tailwind', 'JavaScript', 'CSS'];

interface Tag {
  id: number;
  text: string;
}

export default function DynamicInput() {
return <></>
}
